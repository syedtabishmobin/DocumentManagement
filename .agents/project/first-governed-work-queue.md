# First governed work queue

This queue recommends dependency/risk order; GitHub Issues remains authoritative. Each application item must be shaped against the existing stable story/requirement/decision IDs before implementation.

1. **Completed — remote repository enforcement (`DEC-FWK-001`, [Issue #1](https://github.com/syedtabishmobin/DocumentManagement/issues/1)).** The Product Authority selected public visibility. Protected `main`, strict required Phase 1 checks, linear history, resolved conversations, administrator enforcement and branch cleanup are enabled. Required approvals remain zero until a second authorised reviewer exists. GitHub Environment wiring remains later release-control work.
2. **Completed — transactional durable authority persistence ([Issue #2](https://github.com/syedtabishmobin/DocumentManagement/issues/2), [PR #4](https://github.com/syedtabishmobin/DocumentManagement/pull/4)).** Independent QA passed `AUTH-DUR-AC-01` through `AUTH-DUR-AC-10` after defects #5–#8 were fixed and independently retested. The PostgreSQL adapter, canonical migrations, transaction/idempotency/concurrency controls, synthetic import/verification/repair and durable authority audit/outbox boundary are merged without provider activation. Azure provisioning/conformance, backup/restore and production gates remain future activation/release work.

**Observability boundary:** Agent Operations & Observability is governed by [Issue #11](https://github.com/syedtabishmobin/DocumentManagement/issues/11). Issue #18 completed live ACS Email conformance, exact-candidate independent QA, protected CI and merge; autonomous readiness is `PASS`. This document recommends the next queue only. Do not start items 3–6 without an explicit subsequent governed request.
3. **Complete current-authorization coverage.** Extend authorization to fields, evidence, graph edges, search, answers/citations, tasks, exports and activity, with delegated grants, revocation propagation, minimal disclosure and side-channel evidence (`STORY-P1-003`, `019`–`023`, `038`, `039`).
4. **Client encryption and immutable artifact lifecycle.** Implement reviewed envelope contracts, secure web/Flutter device keys, customer/workspace key grants, immutable Azure Blob artifacts, logical versions, integrity, rotation/revocation and crypto-shred (`DEC-050`; `STORY-P1-004`, `007`, `008`, `011`, `039`, `044`, `045`). Split contract/key-custody and storage work into isolated branches after shared interfaces are approved.
5. **Durable ingestion, quarantine and audit.** Build transactional outbox/lease/retry/dead-letter/reconciliation, trustworthy content inspection, encrypted quarantine, and append-only privacy-safe audit (`STORY-P1-004`–`006`, `009`, `015`).
6. **Environment/release control wiring.** Provision Stage only within `DEC-054`, bind workflows to governed environments, implement immutable candidate evidence and independent Stage QA/BA/UAT gates. Production stays unprovisioned.
7. **Completed — Product Authority notification adapter ([Issue #18](https://github.com/syedtabishmobin/DocumentManagement/issues/18)).** The prepared ACS infrastructure now operates behind the vendor-neutral contract with recipient allow-list, managed-identity send, minimised packets, atomic deduplication, bounded retry, terminal reconciliation, diagnostics and live blocking/UAT conformance evidence.

Connectors and hosted processing remain `CONFIGURED_DISABLED` until their own contract, authorization, consent, token-custody, deletion, security/privacy, and release evidence pass.
