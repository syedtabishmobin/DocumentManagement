{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-engineering-framework.invalid/protocols/notification-event.schema.json",
  "title": "Deduplicated Product Authority notification event",
  "type": "object",
  "required": ["key", "eventType", "projectId", "subject", "authoritativeUrl", "requiredAction", "reason", "appliesTo", "recommendation", "alternatives", "blocked", "continuing", "remaining"],
  "properties": {
    "key": {"type": "string", "pattern": "^[a-z0-9][a-z0-9._:-]+$"},
    "eventType": {"enum": ["BLOCKING_DECISION", "UAT_READY"]},
    "projectId": {"type": "string"},
    "subject": {"type": "string", "minLength": 1, "maxLength": 200},
    "authoritativeUrl": {"type": "string", "format": "uri"},
    "requiredAction": {"type": "string", "minLength": 1, "maxLength": 20000},
    "reason": {"type": "string", "minLength": 1, "maxLength": 20000},
    "appliesTo": {"type": "array", "minItems": 1, "maxItems": 20, "items": {"type": "string", "minLength": 1, "maxLength": 2000}},
    "recommendation": {"type": "string", "minLength": 1, "maxLength": 20000},
    "alternatives": {"type": "array", "minItems": 1, "maxItems": 20, "items": {"type": "string", "minLength": 1, "maxLength": 2000}},
    "blocked": {"type": "array", "minItems": 1, "maxItems": 20, "items": {"type": "string", "minLength": 1, "maxLength": 2000}},
    "continuing": {"type": "array", "maxItems": 20, "items": {"type": "string", "minLength": 1, "maxLength": 2000}},
    "remaining": {"type": "array", "minItems": 1, "maxItems": 20, "items": {"type": "string", "minLength": 1, "maxLength": 2000}},
    "releaseEvidence": {"type": "string"},
    "accessInstructions": {"type": "string"},
    "deliveredScope": {"type": "array", "minItems": 1, "maxItems": 20, "items": {"type": "string", "minLength": 1, "maxLength": 2000}},
    "acceptanceSummary": {"type": "string", "minLength": 1, "maxLength": 20000},
    "residualRisks": {"type": "array", "maxItems": 20, "items": {"type": "string", "minLength": 1, "maxLength": 2000}},
    "businessAcceptanceStatus": {"enum": ["PASS", "FAIL", "BLOCKED", "NOT_STARTED"]},
    "recommendedUatScenarios": {"type": "array", "minItems": 1, "maxItems": 20, "items": {"type": "string", "minLength": 1, "maxLength": 2000}}
  },
  "allOf": [
    {
      "if": {"properties": {"eventType": {"const": "UAT_READY"}}},
      "then": {"required": ["releaseEvidence", "accessInstructions", "deliveredScope", "acceptanceSummary", "residualRisks", "businessAcceptanceStatus", "recommendedUatScenarios"]}
    }
  ],
  "additionalProperties": false
}
